server:
  port: 8090

eureka:  
  client:
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: courses-service
          uri: lb://courses-service
          predicates:
            - Path=/courses/**
